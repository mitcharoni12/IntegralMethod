fileID = fopen("integralData.txt", "r");
integralVals = fscanf(fileID, '%d', 100);
fclose(fileID);

fileID = fopen("integralBinData.txt");
xVals = fscanf(fileID, '%f', 100);
fclose(fileID);

data = [xVals, integralVals];
start = 

integralMethodFunction = @(xVals, lambda) ... 
    (1-e^(-lambda*xVals));

phat = mle(data, 'nloglf', integralMethodFunction, 'start', [0.05 1.0]);

disp(phat)